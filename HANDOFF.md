# HANDOFF — T05 템플릿 가져오기 값 검사

## 1. 목표

index.html의 템플릿 JSON 가져오기 기능에서 필수 항목이 존재하는지만 검사하지 않고, 값의 유효성까지 검사한다.

유효한 값 기준:

- ratio = "1:1" · "4:5" · "9:16"
- text = 문자열
- size = 숫자 20~160
- color = "#RRGGBB"
- align = "left" · "center" · "right"

잘못된 템플릿이 하나라도 있으면 전체를 저장하지 않아야 한다.
오류 메시지에는 반드시 **N번째 템플릿**과 따옴표로 감싼 항목 이름이 포함되어야 한다.

예: `1번째 템플릿의 "ratio" 값이 올바르지 않습니다`

## 2. 현재 상태

- 기준 커밋: `e8d79d3627ac76b2e48df515dc9398f11915bfdf`
- 마지막 검사 결과: `T05-SUITE-v1 | 2026-09-18 08:40 KST | 8/10 PASS | FAIL: T05-06, T05-10`

현재 `validateBundle` 함수에 다음 값 검사가 추가되어 있다: ratio, text, color, align

이미 통과한 검사: T05-01, T05-02, T05-03, T05-04, T05-05, T05-07, T05-08, T05-09
남은 검사는 size 관련 2개뿐이다.

## 3. 실행 명령

### 1) 로컬 서버로 실행

저장소 폴더에서 다음 명령을 실행한다.

```
py -m http.server 8000
```

또는:

```
python -m http.server 8000
```

그다음 브라우저에서 `http://127.0.0.1:8000/tests.html` 을 연다.

### 2) GitHub Pages에서 실행

다음 검사 페이지를 연다: https://dochihs.github.io/t05-relay/tests.html
페이지에서 [검사 실행] 버튼을 누른다.

## 4. 통과 검사

| ID | 입력 | 기대값 | 현재 |
|---|---|---|---|
| T05-01 | 정상 템플릿 3개 | "가져왔습니다" · 기존 1개 → 4개 | PASS |
| T05-02 | 문법이 깨진 JSON | "문법이 손상된 JSON" · 기존 1개 그대로 | PASS |
| T05-03 | 2번째 템플릿에 color 항목 없음 | "2번째 템플릿" + "color" · 기존 1개 그대로 | PASS |
| T05-04 | ratio가 "3:2" | "1번째 템플릿" + "ratio" · 기존 1개 그대로 | PASS |
| T05-05 | color가 "red" | "1번째 템플릿" + "color" · 기존 1개 그대로 | PASS |
| T05-06 | size가 문자열 "크게" | "1번째 템플릿" + "size" · 기존 1개 그대로 | FAIL |
| T05-07 | text가 숫자 123 | "1번째 템플릿" + "text" · 기존 1개 그대로 | PASS |
| T05-08 | align이 "middle" | "1번째 템플릿" + "align" · 기존 1개 그대로 | PASS |
| T05-09 | size 경계값 20과 160인 템플릿 2개 | "가져왔습니다" · 기존 1개 → 3개 | PASS |
| T05-10 | 3번째 템플릿의 size가 161 | "3번째 템플릿" + "size" · 기존 1개 그대로 | FAIL |

## 5. 남은 문제

첫 번째 실패 검사는 T05-06이다. 입력은 `size: "크게"`.
현재 `validateBundle`에는 size의 값 검사가 아직 없다. 따라서 size가 문자열이어도 현재는 유효한 템플릿으로 판단하여 저장하려고 한다.

T05-10도 같은 원인으로 실패한다. 입력은 `size: 161`.

필요한 기준은 size = 숫자이며 20 이상 160 이하.
즉, `typeof ... === 'number'` 검사와 20~160 범위 검사가 모두 필요하다.

## 6. 다음 행동

다음 작업자는 먼저 다음 순서로 진행한다.

1. index.html의 `validateBundle` 함수를 확인한다.
2. 기존 ratio, text, color, align 검사는 그대로 두고 size 값 검사를 추가한다.
3. tests.html을 수정하지 않고 T05 전체 10개를 다시 실행하여 10/10 PASS인지 확인한다.

size 오류 메시지는 기존 형식과 동일하게 `N번째 템플릿의 "size" 값이 올바르지 않습니다` 형태가 되어야 한다.

## 7. 건드리지 말 것

- tests.html은 절대 수정하지 않는다.
- 검사 항목을 삭제하거나 느슨하게 만들지 않는다.
- 기대값을 변경하지 않는다.
- 기존 JSON 문법 오류 처리인 "문법이 손상된 JSON" 안내를 변경하지 않는다.
- 기존 오류 처리의 "기존 템플릿 N개는 그대로 있습니다" 문구를 변경하지 않는다.
- validateBundle의 기존 필수 항목 존재 검사 로직을 삭제하지 않는다.
- 이미 추가된 ratio 값 검사를 변경하지 않는다.
- 이미 추가된 text 값 검사를 변경하지 않는다.
- 이미 추가된 color 값 검사를 변경하지 않는다.
- 이미 추가된 align 값 검사를 변경하지 않는다.
- 이번 작업에서는 size 검사만 추가한다.
- 잘못된 템플릿이 하나라도 있으면 일부 템플릿만 저장해서는 안 된다.
